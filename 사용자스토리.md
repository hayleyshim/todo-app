# 할 일 관리 앱 — 사용자 스토리 & 수락조건

- 문서 버전: v1.0
- 작성일: 2026-06-23
- 기준 문서: `요구사항정의서.md` v2.0, `화면흐름설계.md` v1.0
- 형식: `~로서, ~하고 싶다, ~하기 위해` + 수락조건(Given/When/Then)

> **등급 표기** — [MVP] 1차 필수 / [2차] 후순위 / [보류] 검증 후 판단
> 각 스토리는 요구사항 3장의 핵심가치와 연결된다:
> ① 놓치지 않게 ② 적는 게 일이 안 되게 ③ 머릿속 비우기(안심)

---

## 에픽 1. 계정 / 로그인

### US-1.1 소셜 로그인 [MVP]
**할 일을 관리하려는 사용자로서**, 복잡한 가입 절차 없이 **소셜 계정으로 바로 로그인하고 싶다**,
앱을 빠르게 시작하기 위해.

**수락조건**
- [ ] Given 로그인 화면에서, When [Google로 계속하기]를 누르면, Then Google 인증 후 할 일 목록(홈)으로 이동한다.
- [ ] Given 첫 로그인 사용자, When 인증에 성공하면, Then 별도 회원가입 단계 없이 계정이 자동 생성된다.
- [ ] Given 이미 로그인한 사용자, When 앱을 다시 켜면, Then 로그인 화면을 건너뛰고 홈으로 바로 진입한다.
- [ ] Given 인증이 실패/취소되면, When 로그인 화면으로 돌아오면, Then 오류 안내 메시지가 표시된다.

### US-1.2 로그아웃 [MVP]
**사용자로서**, 설정에서 **로그아웃하고 싶다**, 계정을 전환하거나 보호하기 위해.

**수락조건**
- [ ] Given 설정 화면에서, When [로그아웃]을 누르면, Then 확인 후 로그인 화면으로 이동한다.
- [ ] Given 로그아웃 상태에서, When 앱을 켜면, Then 로그인 화면이 표시된다.

---

## 에픽 2. 빠른 입력 (핵심가치 ②)

### US-2.1 제목만으로 빠른 추가 [MVP] ⭐
**바쁜 사용자로서**, 떠오른 할 일을 **제목만 입력해 즉시 등록하고 싶다**,
적는 것이 일이 되지 않게 하기 위해.

**수락조건**
- [ ] Given 홈 화면 하단에 빠른 추가 입력란이 항상 보인다.
- [ ] Given 입력란에 제목을 입력하고, When 엔터/추가를 누르면, Then 다른 화면 이동 없이 목록에 즉시 추가된다.
- [ ] Given 제목만 입력한 경우, When 등록되면, Then 우선순위는 "보통", 마감/반복은 없음으로 저장된다.
- [ ] Given 추가 직후, When 입력란이 비워지면, Then 연속으로 다음 할 일을 바로 입력할 수 있다.
- [ ] 제목 입력 시작부터 등록 완료까지 **3초 이내**에 가능하다(다른 필드 강요 없음).
- [ ] Given 제목이 비어 있으면, When 추가를 누르면, Then 등록되지 않는다(빈 항목 방지).

### US-2.2 음성으로 추가 [2차]
**손이 자유롭지 않은 사용자로서**, **음성으로 할 일을 추가하고 싶다**, 더 빠르게 기록하기 위해.

**수락조건**
- [ ] Given 입력란의 마이크 버튼을 누르면, When 말하면, Then 인식된 텍스트가 제목으로 채워진다.
- [ ] Given 인식 결과를, When 확인/수정 후 추가하면, Then 일반 할 일과 동일하게 저장된다.

### US-2.3 자연어로 마감일 인식 [2차]
**사용자로서**, "내일 오후 3시 약 먹기"처럼 **자연어로 입력하면 마감일이 자동 인식되길 원한다**, 입력을 더 줄이기 위해.

**수락조건**
- [ ] Given "내일 오후 3시 약 먹기"를 입력하면, When 등록하면, Then 제목="약 먹기", 마감="내일 15:00"으로 분리 저장된다.
- [ ] Given 날짜 표현이 없으면, When 등록하면, Then 마감 없이 제목만 저장된다.
- [ ] 잘못 인식된 경우 사용자가 등록 전에 수정할 수 있다.

---

## 에픽 3. 할 일 관리 (CRUD)

### US-3.1 할 일 상세 입력/편집 [MVP]
**사용자로서**, 할 일에 **마감일·반복·우선순위를 덧붙이거나 수정하고 싶다**, 필요한 것만 관리하기 위해.

**수락조건**
- [ ] Given 목록에서 항목을 탭하면, When 편집 바텀시트가 열리면, Then 제목·마감·반복·우선순위를 볼 수 있다.
- [ ] Given 편집 화면에서, When 값을 바꾸고 저장하면, Then 목록에 즉시 반영된다.
- [ ] Given 상세 필드(메모 등)는, When 화면을 열면, Then "더보기"로 접혀 있어 기본 화면이 단순하다.
- [ ] 마감을 설정하면 "알림 보내기"가 기본 ON으로 제안된다.

### US-3.2 할 일 완료 / 완료 취소 [MVP]
**사용자로서**, 끝낸 일을 **완료 처리하고 되돌릴 수도 있고 싶다**, 진행 상황을 정리하기 위해.

**수락조건**
- [ ] Given 목록 항목의 체크박스를, When 탭하면, Then 완료 표시(줄긋기) 후 "완료됨" 섹션으로 이동한다.
- [ ] Given 완료 직후, When 되돌리기 토스트를 누르면, Then 미완료로 복원된다.
- [ ] Given 완료된 항목을, When 다시 탭하면, Then 미완료로 되돌아간다.
- [ ] "완료됨" 섹션은 기본적으로 접혀 있다.

### US-3.3 할 일 삭제 [MVP]
**사용자로서**, 필요 없는 할 일을 **삭제하고 싶다**, 목록을 깔끔하게 유지하기 위해.

**수락조건**
- [ ] Given 항목을 스와이프/메뉴로, When 삭제하면, Then 목록에서 제거된다.
- [ ] Given 삭제 직후, When 되돌리기 토스트를 누르면, Then 항목이 복원된다.
- [ ] Given 반복 할 일을 삭제하면, When 확인하면, Then "이 항목만/전체 반복"을 선택할 수 있다.

### US-3.4 우선순위 보고 정렬 [MVP] (핵심가치 ①)
**할 일이 많은 사용자로서**, **우선순위를 한눈에 보고 정렬하고 싶다**, 뭐부터 할지 몰라 멈추지 않기 위해.

**수락조건**
- [ ] Given 각 항목은, When 목록에 표시되면, Then 우선순위를 색상 점(🔴높음/🟡보통/⚪낮음)으로 보여준다.
- [ ] Given 빠른 추가로 만든 항목은, When 저장되면, Then 우선순위 기본값 "보통"이다.
- [ ] Given 편집에서 우선순위를, When 한 번의 탭으로 바꾸면, Then 즉시 반영·정렬된다.
- [ ] 목록 기본 정렬은 "마감 임박 + 우선순위 높은 순"이다.
- [ ] 우선순위 입력은 강요되지 않는다(미지정 시 자동 "보통").

---

## 에픽 4. 마감일 / 똑똑한 알림 (핵심가치 ① — 앱의 심장)

### US-4.1 마감일·시간 설정 [MVP]
**사용자로서**, 할 일에 **마감 날짜와 시간을 정하고 싶다**, 제때 알림을 받기 위해.

**수락조건**
- [ ] Given 편집 화면에서, When 마감 날짜·시간을 선택하면, Then 항목에 저장되고 목록에 표시된다.
- [ ] Given 마감이 지난 항목은, When 목록에 표시되면, Then 시각적으로 구분(예: 빨강)된다.

### US-4.2 마감 시각 푸시 알림 [MVP] ⭐
**자주 잊는 사용자로서**, 마감 시각에 **푸시 알림을 받고 싶다**, 앱을 안 열어도 놓치지 않기 위해.

**수락조건**
- [ ] Given 마감과 알림이 설정된 항목은, When 마감 시각이 되면, Then 앱이 꺼져 있어도 기기 푸시 알림이 도착한다.
- [ ] Given 알림이, When 전송되면, Then 빠짐없이 도착해야 한다(예약 알림 신뢰도).
- [ ] Given 알림 본문을, When 탭하면, Then 해당 항목으로 앱이 열린다.
- [ ] Given 항목을 완료/삭제하면, When 처리되면, Then 예약된 알림이 취소된다.

### US-4.3 알림에서 바로 완료 / 미루기 [MVP] ⭐
**바쁜 사용자로서**, 알림에서 **바로 완료하거나 미루고 싶다**, 앱을 열지 않고 처리하기 위해.

**수락조건**
- [ ] Given 알림에는, When 표시되면, Then [완료]와 [미루기] 액션 버튼이 있다.
- [ ] Given 알림에서 [완료]를 누르면, When 처리되면, Then 해당 할 일이 완료되고 알림이 사라진다.
- [ ] Given [미루기]를 누르면, When 처리되면, Then 설정된 간격(예: 30분) 뒤 재알림이 예약된다.

### US-4.4 미완료 재알림 [2차] (핵심가치 ①)
**놓치기 쉬운 사용자로서**, 처리 안 한 할 일에 대해 **다시 알림받고 싶다**, 끝내 놓치지 않기 위해.

**수락조건**
- [ ] Given 알림 후에도 미완료 상태면, When 정책상 시점이 되면, Then 재알림이 발송된다.
- [ ] 재알림 횟수·간격은 설정에서 조정할 수 있다.

### US-4.5 마감 전 미리 알림 [2차]
**사용자로서**, 마감 **N분/시간 전에 미리 알림받고 싶다**, 준비할 시간을 갖기 위해.

**수락조건**
- [ ] Given 미리 알림 간격을 설정하면, When 마감 전 해당 시점이 되면, Then 미리 알림이 도착한다.

---

## 에픽 5. 반복 할 일 (핵심가치 ① — 가장 자주 놓치는 영역)

### US-5.1 반복 주기 설정 [MVP] ⭐
**규칙적인 일을 하는 사용자로서**, 할 일을 **매일/매주/매월 반복으로 설정하고 싶다**, 매번 새로 적지 않기 위해.

**수락조건**
- [ ] Given 편집 화면에서, When 반복(매일/매주 요일/매월 날짜)을 선택하면, Then 항목에 저장된다.
- [ ] Given 반복 항목은, When 목록에 표시되면, Then 반복 표시(예: "매주 화")가 보인다.

### US-5.2 반복 항목 자동 생성 [MVP]
**사용자로서**, 반복 할 일을 완료하면 **다음 회차가 자동으로 생기길 원한다**, 끊김 없이 이어가기 위해.

**수락조건**
- [ ] Given 반복 항목을 완료하면, When 처리되면, Then 다음 주기의 항목이 자동 생성/갱신된다.
- [ ] Given 다음 회차 항목은, When 생성되면, Then 동일한 알림 설정으로 푸시 예약된다.

### US-5.3 반복 예외 처리 [2차]
**사용자로서**, 특정 회차를 **건너뛰거나 한 번만 수정하고 싶다**, 예외 상황에 대응하기 위해.

**수락조건**
- [ ] Given 반복 항목 한 회차를, When 건너뛰면, Then 그 회차만 사라지고 반복은 유지된다.
- [ ] Given 한 회차만 수정하면, When 저장하면, Then 나머지 회차에는 영향이 없다.

---

## 에픽 6. 목록 / 홈 화면

### US-6.1 오늘 할 일 목록 보기 [MVP]
**사용자로서**, 홈에서 **할 일을 한 화면에서 보고 싶다**, 흩어진 일을 한곳에 모으기 위해.

**수락조건**
- [ ] Given 홈 화면은, When 진입하면, Then 미완료 할 일을 정렬된 리스트로 보여준다.
- [ ] Given 완료된 항목은, When 표시되면, Then 별도 "완료됨" 섹션(기본 접힘)에 모인다.
- [ ] Given 할 일이 하나도 없으면, When 진입하면, Then 빈 상태 안내와 입력 유도가 표시된다.

### US-6.2 정렬 / 분리 표시 [MVP]
**사용자로서**, 목록이 **마감·우선순위 기준으로 정렬되길 원한다**, 중요한 일을 먼저 보기 위해.

**수락조건**
- [ ] Given 목록은, When 표시되면, Then 마감 임박 + 우선순위 높은 순으로 정렬된다.
- [ ] 완료/미완료가 시각적으로 분리된다.

### US-6.3 달력 뷰 [2차]
**사용자로서**, 할 일을 **달력에서 날짜별로 보고 싶다**, 일정 분포를 파악하기 위해.

**수락조건**
- [ ] Given 목록↔달력 토글이 있고, When 달력을 선택하면, Then 날짜별 할 일이 배치되어 보인다.
- [ ] 목록 뷰 검증 후 도입한다.

---

## 에픽 7. 오프라인 / 동기화 (비기능)

### US-7.1 오프라인 사용 [MVP]
**이동 중인 사용자로서**, 인터넷이 없어도 **할 일을 보고 추가/수정하고 싶다**, 언제든 기록하기 위해.

**수락조건**
- [ ] Given 네트워크가 없는 상태에서, When 할 일을 추가/수정/완료하면, Then 로컬에 정상 저장되고 화면에 반영된다.
- [ ] Given 오프라인에서 만든 변경은, When 온라인으로 복귀하면, Then 자동으로 서버와 동기화된다.

### US-7.2 다중 기기 일관성 [2차]
**여러 기기를 쓰는 사용자로서**, 어느 기기에서든 **같은 할 일을 보고 싶다**, 일관되게 관리하기 위해.

**수락조건**
- [ ] Given 한 기기에서 변경하면, When 다른 기기를 열면, Then 동기화된 최신 상태가 보인다.

---

## 에픽 8. 설정

### US-8.1 알림 설정 [MVP]
**사용자로서**, **알림을 켜고 끄거나 기본값을 정하고 싶다**, 내 방식대로 알림받기 위해.

**수락조건**
- [ ] Given 설정에서, When 전체 알림을 OFF하면, Then 새/기존 푸시 알림이 발송되지 않는다.
- [ ] 기본 알림 시각, 미루기(스누즈) 간격을 설정할 수 있다.
- [ ] Given OS 알림 권한이 없으면, When 설정에 진입하면, Then 권한 요청/안내가 표시된다.

---

## 부록. 등급별 요약

| 에픽 | MVP | 2차 | 보류 |
|------|-----|-----|------|
| 1 계정/로그인 | US-1.1, 1.2 | — | — |
| 2 빠른 입력 | US-2.1 | US-2.2, 2.3 | — |
| 3 할 일 관리 | US-3.1~3.4 | — | 카테고리/태그 |
| 4 마감/알림 | US-4.1~4.3 | US-4.4, 4.5 | — |
| 5 반복 | US-5.1, 5.2 | US-5.3 | — |
| 6 목록/홈 | US-6.1, 6.2 | US-6.3(달력) | — |
| 7 오프라인 | US-7.1 | US-7.2 | — |
| 8 설정 | US-8.1 | — | — |

> ⭐ = 핵심가치를 직접 실현하는 "반드시 잘 만들어야 하는" 스토리:
> US-2.1(빠른 추가), US-4.2·4.3(알림에서 처리), US-5.1(반복).
