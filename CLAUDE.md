# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 이 저장소의 성격

코드 저장소가 아니라 **Android 할 일 관리 앱의 기획·설계 문서 모음**이다. 아직 구현 코드는 없으며,
모든 산출물은 한국어 Markdown 문서와 1개의 저충실도 HTML 와이어프레임이다.
빌드/테스트/린트 시스템은 없다.

- 와이어프레임 미리보기: `open wireframe.html` (브라우저로 바로 열림, 빌드 불필요)
- 모든 신규 문서·주석은 **한국어**로 작성한다.

## 문서 구조와 의존 관계 (가장 중요)

문서들은 독립적이지 않고 **상류 → 하류로 이어지는 사슬**이다. 하류 문서를 수정할 때는
반드시 상류 문서의 결정을 따라야 하며, 상류를 바꾸면 하류 전체에 전파해야 한다.

```
요구사항정의서.md  (왜 만드는가 — 문제·목적·핵심가치·기능등급)
      │
      ▼
사용자스토리.md    (무엇을 — 에픽/스토리 + Given/When/Then 수락조건)
      │
      ▼
화면흐름설계.md    (화면 간 이동 — 흐름도·시나리오)
화면구성설계.md    (화면 내부 — 레이아웃·컴포넌트, 스토리↔화면 매핑)
      │
      ▼
wireframe.html     (UX 키스크린 — 시각 디자인 배제, 구조만)
```

각 문서 상단에는 `기준 문서:` 줄로 자신이 어떤 상류 문서의 어느 버전에 근거하는지 명시한다.
문서를 고치면 이 버전 표기와 하류 문서의 정합성을 함께 갱신한다.

## 제품을 지배하는 3대 핵심가치

`요구사항정의서.md` 2~3장은 실제 고객 발견 인터뷰에서 도출됐다. **모든 기능·화면·스토리는
아래 세 가치 중 하나를 실현하는 것으로 정당화돼야 한다.** 이를 해치는 제안은 거부한다.

1. **놓치지 않게 챙겨준다** — 사용자가 다시 안 봐도 앱이 먼저 알린다. 핵심 경험은 화면이 아니라
   **푸시 알림**에 있다(앱을 안 열어도 가치가 전달돼야 함).
2. **적는 게 일이 되지 않는다** — 제목만으로 3초 내 등록. 우선순위·마감·태그 등은 전부 **선택**이며
   기본값을 제공한다. 입력을 강요하는 순간 이 가치를 위반한다.
3. **머릿속을 비워 안심시킨다** — 적어두면 잊어도 된다는 신뢰.

파생 결과로 기억할 것:
- 페르소나는 이미 **종이 달력으로 실패**했다 → 달력 뷰는 핵심 해법이 아니며 2차로 미룬다.
- 우선순위는 MVP에 포함하되 **기본값 "보통"** 으로 입력을 강요하지 않는다(가치 2 보호).
- MVP는 하단 탭바 없이 **홈 단일 화면 + 바텀시트** 구조다(단순함 유지).

## 기능 우선순위 등급

모든 기능/스토리/필드에는 `[MVP] / [2차] / [보류]` 등급이 붙어 있다.
- **[MVP]** 빠른 입력 · 똑똑한 알림(알림에서 완료/미루기) · 반복 할 일 · 우선순위 · 소셜 로그인 · 오프라인
- **[2차]** 달력 뷰 · 메모 · 음성/자연어 입력 · 재알림 · 다중기기 동기화
- **[보류]** 카테고리/태그 (입력 마찰을 키워 가치 2와 충돌 → 사용자 검증 후 판단)

기능을 추가/이동할 때는 이 등급 체계를 유지하고, 관련 문서(요구사항 5·6·9장, 사용자스토리 부록,
화면구성설계 매핑표)에 일관되게 반영한다.

## 향후 구현 시 결정된 방향 (아직 미구현, 권고안)

`요구사항정의서.md` 8장의 추천 스택이며 확정 코드는 없다:
- Kotlin + Jetpack Compose, MVVM + Repository
- 인증/동기화/푸시: Firebase(Authentication·Firestore·FCM) — 학습 부담 최소화
- 알림 예약: WorkManager / AlarmManager, 오프라인 우선(Offline-first)

## 작업 방식

- 사용자는 **질문을 많이 받는 것**을 선호한다(특히 기획 단계). 가정으로 채우기 전에 묻는다.
- 새 산출물을 만들 땐 위 문서 사슬의 어느 단계인지 밝히고, 상류 문서와의 정합성을 먼저 확인한다.
- 미해결 사항은 각 문서 말미의 "미해결/다음 단계" 절에 누적한다.
